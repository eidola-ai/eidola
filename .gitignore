.env
.env.*

# Nix build outputs
result
result-*

# direnv (.envrc.local is the machine-local hook sourced by .envrc)
.direnv
.envrc.local

### Rust
debug/
target/
.cargo/

# These are backup files generated by rustfmt
**/*.rs.bk

# Local .app bundle output
crates/eidola-gui/build/

# Test artifacts
test-artifacts/

# Visual snapshots from the gpui app — local-only debug aid, not regression
# goldens. The harness regenerates them; cross-machine pixels aren't stable.
crates/eidola-gui/tests/snapshots/

# macOS
.DS_Store

.buildx-cache
.dev-certs/

# Brand asset generator scratch
__pycache__/
brand/.render/
brand/.render-*/
brand/.iconset-*/
brand/.preview/

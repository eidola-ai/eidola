# Copy to .env and fill in values. The .env file is gitignored.
TINFOIL_API_KEY=
DATABASE_URL=postgres://eidolons@localhost/eidolons
STRIPE_API_KEY=
STRIPE_WEBHOOK_SECRET=
DSTACK_SIMULATOR_ENDPOINT=http://localhost:8090
TLS_SANS=localhost,server
# Optional: JSON overrides for model pricing (see backend.rs MODEL_CATALOG for defaults).
# Example: TINFOIL_PRICING_OVERRIDES={"kimi-k2-5":{"input":2.0,"output":6.0}}
# TINFOIL_PRICING_OVERRIDES=

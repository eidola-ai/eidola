[package]
name = "eidolons"
version = "0.1.0"
edition = "2024"

[dependencies]
uniffi = "0.30.0"

[build-dependencies]
uniffi = { version = "0.30.0", features = ["build"] }

[lib]
crate-type = ["cdylib", "staticlib", "lib"]
